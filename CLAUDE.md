# ClipFarm — for AI agents

Before doing anything in this repo, read these two files — they are the source of truth:

- **[README.md](README.md)** — what the project is, the architecture, the processing
  pipeline, how to run it, and where to make changes.
- **[CONTRIBUTING.md](CONTRIBUTING.md)** — how we work: branch/commit/PR conventions,
  CI + pre-commit, branch protection, and the gotchas (shared-DB migrations, docker env
  reload, `web/` being a non-standard Next.js).

Do not restate their contents here — follow them. This file exists so agents that
auto-load `CLAUDE.md` land on the conventions without being told.
