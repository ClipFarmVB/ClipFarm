// Placeholder for CF-330 (#380) — Upload.
// Replace this file; the route and its options are already registered.
import { Placeholder } from "@/components/placeholder";

export default function UploadScreen() {
  return (
    <Placeholder
      screen="Upload"
      ticket="CF-330 (#380)"
      note="Builds against the mock in src/upload."
    />
  );
}
