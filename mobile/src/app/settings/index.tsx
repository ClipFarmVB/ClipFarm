// Placeholder for CF-343 (#393) — Settings.
// Replace this file; the route and its options are already registered.
import { Placeholder } from "@/components/placeholder";

export default function SettingsScreen() {
  return (
    <Placeholder
      screen="Settings"
      ticket="CF-343 (#393)"
      note="Wifi-only upload toggle (CF-327) and sign out live here."
    />
  );
}
